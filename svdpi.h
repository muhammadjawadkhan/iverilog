/* Minimal svdpi.h for this fork's DPI-C import slice (scalar int).
 * Full Accellera SVDPI types are not required for import "DPI-C"
 * functions that only pass C int / SV int.
 */
#ifndef IVL_SVDPI_H
#define IVL_SVDPI_H

#ifdef __cplusplus
extern "C" {
#endif

/* Intentionally minimal — extend when open arrays / strings land. */

#ifdef __cplusplus
}
#endif

#endif /* IVL_SVDPI_H */
